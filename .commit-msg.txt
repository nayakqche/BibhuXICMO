feat(ig): QuickAds-style InfluencerFind as primary IG agent view

Reshape /agents/instagram around seed-account influencer discovery
(mirrors quickads-instagram-reachout-automation.onrender.com):

- Hero block + Search Influencers card with seed accounts textarea,
  min/max follower band, and optional category filter
- Progress bar + "Starting scraper…" status during the run
- DM Outreach Template editor with {name}/{username}/{followers}/{category}
  chip insertion and Live Preview
- Results table: Profile, Full Name, Followers, Engagement, Quality (color
  badge), Email, Category, Bio, Actions (Send DM / Open / Remove)
- Per-row "DM" button copies templated message + opens IG profile in a new
  tab; "DM All" walks rows with 1.8s stagger; CSV + Excel (HTML .xls) export
- New server actions: runIGSeedDiscovery, recordIGDmSent, deleteIGCreator,
  clearIGCreators
- New backend pipeline discoverFromSeeds(): scrape seeds → mine top
  hashtags from bios+latest posts → fan-out via hashtag scraper → batch
  profile scrape → filter by follower band → heuristic quality sort
- Apify profile normalizer now extracts email (regex on bio + business
  email fields), business category, latest-12 ER, and a 0-100 quality
  score (engagement + follower/following ratio + verified/business + email
  + posts activity)
- IGCreator schema: +email, +category, +qualityScore, +following,
  +postsCount, +isVerified, +isBusiness, +externalUrl
- Add minimal Textarea + Progress UI primitives
- Keep existing Posts / Reels / Stories / Replies / Discovered /
  Campaigns / Composer / Runs tabs under a "Content & engagement"
  section below the main view
